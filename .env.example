NODE_ENV=
PORT=
TZ=

POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST_PORT=
DATABASE_URL=

OPENAI_API_KEY=

# Phase 3: アプリ側 Bearer Token（正本は apps/api/.env.example / Vercel 環境変数）
API_BEARER_TOKEN=

# Neon 移行後の DATABASE_URL / DIRECT_URL は apps/api/.env.example を参照（Prisma の正本はそちら）
